# Steam bot credentials and runtime files
.env
**/secrets.txt

# Old project (reference only, not part of new codebase)
inhouse-buddy/

# Claude internal
.claude/

# SQLite — database directory and any stray db files
data/
*.db
*.db-shm
*.db-wal

# Compiled binaries
inhouse-buddy-bot
gsi/gsi-receiver
/packets
/capture
